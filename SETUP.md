# 🌸 Открытка-Квест на 8 марта — полное руководство

Интерактивное веб-приложение для создания и отправки персональных открыток с мини-квестами на 8 марта.

## 🚀 Начало работы

### 1. Требования

- **Node.js** 18+ и npm 9+
- **Supabase аккаунт** (бесплатный тарифный план подходит)
- **Vercel аккаунт** (для деплоя, опционально)

### 2. Локальная установка

```bash
# Клонирование/Переход в проект
cd postcards-quest

# Установка зависимостей
npm install

# Создание .env.local с ключами Supabase
# Скопируй значения из панели Supabase (см. шаг 3)
```

### 3. Настройка Supabase

#### 3.1 Создание проекта
1. Перейди на https://app.supabase.com
2. Нажми "New Project"
3. Выбери регион, введи пароль (сохрани его!)
4. Дождись завершения инициализации

#### 3.2 SQL Setup

Перейди в **SQL Editor** и склей оба скрипта по порядку:

**Скрипт 1: Создание таблицы**
```sql
create table public.postcards (
  id uuid primary key default gen_random_uuid(),
  created_at timestamp with time zone default now(),
  sender_name text,
  recipient_name text,
  message text not null,
  compliments text[] not null,
  theme text not null default 'pastel',
  mode text not null default 'cute',
  quiz_seed int not null,
  secret_word text,
  view_count int default 0
);

create index postcards_created_at_idx on postcards(created_at);
create index postcards_sender_name_idx on postcards(sender_name);
```

**Скрипт 2: RLS Политики**
```sql
-- Включить RLS
alter table public.postcards enable row level security;

-- Разрешить INSERT для всех (anon)
create policy "Allow anonymous insert"
  on public.postcards
  for insert
  with check (true);

-- Разрешить SELECT только текущего пользователя (по id)
create policy "Allow select by id"
  on public.postcards
  for select
  using (true);

-- Запретить UPDATE
create policy "Deny updates"
  on public.postcards
  for update
  with check (false);

-- Запретить DELETE
create policy "Deny deletes"
  on public.postcards
  for delete
  with check (false);
```

#### 3.3 Получение ключей
1. Перейди в **Settings** → **API**
2. Скопируй:
   - `Project URL` → в `.env.local` как `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public` key → в `.env.local` как `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### 4. Файл `.env.local`

Создай файл в корне проекта:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project-xyz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...полный-ключ...
```

### 5. Запуск локально

```bash
npm run dev
```

Приложение откроется на **http://localhost:3000**

---

## 📁 Структура проекта

```
postcards-quest/
├── app/
│   ├── page.tsx                    # Главная страница (лендинг)
│   ├── create/page.tsx             # Создание открытки
│   ├── card/[id]/page.tsx          # Просмотр открытки + квест
│   ├── layout.tsx                  # Root layout
│   └── globals.css                 # Глобальные стили
│
├── components/
│   ├── ThemePicker.tsx             # Выбор темы открытки
│   ├── ModeToggle.tsx              # Cute / Cute+Meme переключатель
│   ├── CardPreview.tsx             # Предпросмотр открытки
│   ├── QuestFlow.tsx               # Квест (3 задания)
│   ├── Bouquet.tsx                 # Букет из 8 комплиментов
│   ├── StickerReactions.tsx        # Мемные реакции
│   ├── DownloadButton.tsx          # Скачивание/печать
│   ├── Confetti.tsx                # Анимация конфетти
│   ├── LoadingSpinner.tsx          # Экран загрузки
│   └── Footer.tsx                  # Подвал
│
├── lib/
│   ├── types.ts                    # TypeScript типы
│   ├── supabaseClient.ts           # Supabase клиент
│   ├── compliments.ts              # 24 шаблона комплиментов
│   ├── memes.ts                    # Мемы и варианты квеста
│   └── utils.ts                    # Утилиты и хелперы
│
├── public/                         # Статические файлы
├── package.json
├── tailwind.config.ts
├── tsconfig.json
├── next.config.js
├── postcss.config.js
├── .env.local
└── .gitignore
```

---

## 🎯 Функциональность

### Страница создания (`/create`)
- ✅ Имя отправителя/получателя (опционально)
- ✅ Основное поздравление (textarea)
- ✅ 8 редактируемых комплиментов
- ✅ Выбор темы: pastel / cats / corporate / meme-lite
- ✅ Режим: cute | cute+meme (с мемными реакциями)
- ✅ Секретное слово для пасхалки (опционально)
- ✅ Предпросмотр в реальном времени
- ✅ Валидация формы

### Страница открытки (`/card/[id]`)
1. **Hero экран** с приглашением к квесту
2. **Квест из 3 заданий:**
   - Задание 1️⃣: Выбери весенний вариант (подснежник vs снег vs дождик)
   - Задание 2️⃣: Угадай настроение (4 опции, одна правильная зафиксирована по seed)
   - Задание 3️⃣: Нажми на сердечко 8 раз (счётчик + анимация)
3. **Раскрытие поздравления** с конфетти 🎉
4. **Букет из 8 лепестков** (комплименты раскрываются по клику)
5. **Мемные реакции** (если mode === 'cute+meme')
6. **Скачивание/Печать** (window.print() fallback)
7. **Пасхалка** — ввод секретного слова → мемный экран

---

## 🎨 Темы оформления

| Тема | Цвета | Насчёт |
|------|-------|--------|
| **pastel** | Розовый → Фиолетовый → Синий | Мягкий и нежный |
| **cats** | Оранжевый → Жёлтый → Розовый | Весёлый и тёплый |
| **corporate** | Зелёный → Синий → Фиолетовый | Лаконичный и чистый |
| **meme-lite** | Лайм → Бирюза → Фуксия | Яркий и молодёжный |

---

## 🔐 RLS Политики — объяснение

| Операция | Кто мо может | Почему |
|----------|------------|--------|
| **INSERT** | Все (anon) | Создание открытки без регистрации |
| **SELECT** | Все | Просмотр открытки если знаешь ID |
| **UPDATE** | Никто | Сохраняет целостность данных |
| **DELETE** | Никто | Предотвращает удаление |

**Безопасность**: без RLS-политик user A мог бы видеть ALL открытки. С RLS SELECT работает только если запрос соответствует политике (сейчас — все могут SELECT, что OK для веб-сайта).

---

## 📊 База данных

### Таблица `postcards`

```
id                  uuid       (первичный ключ, auto-generated)
created_at          timestamp  (время создания)
sender_name         text       (может быть NULL)
recipient_name      text       (может быть NULL)
message             text       (основное поздравление)
compliments         text[]     (массив из 8 комплиментов)
theme               text       (pastel|cats|corporate|meme-lite)
mode                text       (cute|cute_meme)
quiz_seed           int        (для детерминации ответов квеста)
secret_word         text       (хеш или сырой текст секретного слова)
view_count          int        (счётчик просмотров)
```

---

## 🚀 Деплой на Vercel

### Способ 1: Git + Vercel UI
1. Залей проект на GitHub
2. На https://vercel.com/new выбери репо
3. Добавь переменные окружения из `.env.local`
4. Нажми Deploy

### Способ 2: Vercel CLI
```bash
npm install -g vercel
vercel login
vercel --env-file .env.local
```

### Важно
- `NEXT_PUBLIC_*` переменные видны в браузере (это OK, они анонимные Supabase ключи)
- Убедись что RLS-политики правильно настроены в Supabase
- Тестируй функционал перед deploy

---

## 🛠️ Технологический стек

```json
{
  "Framework": "Next.js 15 + App Router",
  "Language": "TypeScript",
  "Styling": "Tailwind CSS 3.4",
  "Animations": "Framer Motion 11",
  "Database": "Supabase (PostgreSQL)",
  "Icons": "Lucide React",
  "Export": "html-to-image + window.print() fallback"
}
```

---

## 📝 Контент

### 24 шаблона комплиментов
Включены в `lib/compliments.ts` — при создании выбираются 8 из 24.

### 12 мем-фраз
Для мемного режима (cute_meme) — включены в `lib/memes.ts`.

### Варианты квеста
- **Задание 1**: 3 вариантов (spring options)
- **Задание 2**: 4 варианта (mood options)
- **Задание 3**: счётчик (8 кликов)

---

## ⚙️ Настройки & Кастомизация

### Изменение количества комплиментов
В `app/create/page.tsx`:
```typescript
const [compliments, setCompliments] = useState<string[]>(() =>
  selectRandomCompliments(8, generateQuizSeed())  // ← здесь цифра 8
);
```

### Изменение секретного слова
В `lib/memes.ts`:
```typescript
export const SECRET_WORD = 'весна';  // ← измени на своё слово
```

### Добавление новой темы
1. Добавь в `Theme` type в `lib/types.ts`
2. Добавь цвета в `getThemeColors()` в `lib/utils.ts`
3. Добавь кнопку в `components/ThemePicker.tsx`

---

## 🐛 Типичные ошибки и решения

| Ошибка | Решение |
|--------|---------|
| `ENOENT: no such file...` при npm install | `npm ci --force` или переустанови node_modules |
| `Supabase credentials not configured` | Проверь `.env.local` и сделай npm run build |
| 404 на `/card/123` | Открытка не существует в БД или ошибка в ID |
| Спам из `@humanwhocodes` warnings | Это нормально, ESLint 8 имеет такие deprecated зависимости |

---

## 📚 Дополнительные ресурсы

- [Next.js документация](https://nextjs.org/docs)
- [Supabase docs](https://supabase.com/docs)
- [Tailwind CSS](https://tailwindcss.com)
- [Framer Motion](https://www.framer.com/motion/)

---

## 💡 Идеи для улучшений (TODO)

- [ ] Аналитика: отслеживание количества пройденных квестов
- [ ] Галерея открыток (топ-популярные)
- [ ] Уведомления по email при прохождении квеста
- [ ] Более сложный экспорт (PNG/PDF с Puppeteer на сервере)
- [ ] Персонализация квеста (выбор вопросов при создании)
- [ ] Интеграция с соц.сетями (шеринг результатов)
- [ ] Тёмная тема для приложения
- [ ] Видеопоздравления (вставка ссылки на YouTube)
- [ ] Мультиязычность (EN/RU/FR)
- [ ] PWA — offline поддержка

---

## 📄 Лицензия

MIT — используй и модифицируй как угодно 💖

---

**Made with 💕 and ✨ для 8 марта**
