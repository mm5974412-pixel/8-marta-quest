# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ

## ✅ Что уже сделано

- ✅ Создана полная структура Next.js проекта
- ✅ Установлены все зависимости (npm install)
- ✅ Написаны все компоненты и страницы
- ✅ Настроены Tailwind CSS, Framer Motion, TypeScript
- ✅ Подготовлены SQL скрипты для Supabase

---

## 🔧 Что нужно сделать ТЫ

### Шаг 1: Создай проект в Supabase

1. Перейди на **https://app.supabase.com**
2. Нажми **"New Project"**
3. Выбери регион (например, Europe/Frankfurt)
4. Введи пароль (сохрани его, он не восстанавливается!)
5. Дождись инициализации (2-3 минуты)

### Шаг 2: Выполни SQL скрипты

1. В панели Supabase перейди в **"SQL Editor"**
2. Нажми **"New Query"**
3. Скопируй и выполни **первый скрипт** (создание таблицы):

```sql
create table public.postcards (
  id uuid primary key default gen_random_uuid(),
  created_at timestamp with time zone default now(),
  sender_name text,
  recipient_name text,
  message text not null,
  compliments text[] not null,
  theme text not null default 'pastel',
  mode text not null default 'cute',
  quiz_seed int not null,
  secret_word text,
  view_count int default 0
);

create index postcards_created_at_idx on postcards(created_at);
create index postcards_sender_name_idx on postcards(sender_name);
```

4. Создай **новую query** и выполни **второй скрипт** (RLS):

```sql
-- Включить RLS
alter table public.postcards enable row level security;

-- Разрешить INSERT для всех (anon)
create policy "Allow anonymous insert"
  on public.postcards
  for insert
  with check (true);

-- Разрешить SELECT
create policy "Allow select by id"
  on public.postcards
  for select
  using (true);

-- Запретить UPDATE
create policy "Deny updates"
  on public.postcards
  for update
  with check (false);

-- Запретить DELETE
create policy "Deny deletes"
  on public.postcards
  for delete
  with check (false);
```

### Шаг 3: Получи ключи доступа

1. В панели Supabase перейди в **Settings → API** (левая боковая панель)
2. Скопируй:
   - **Project URL** (например: `https://xyzabc.supabase.co`)
   - **anon public** (начинается с `eyJ...`)

### Шаг 4: Настрой переменные окружения

1. Открой файл `.env.local` в корне проекта
2. Замени значения на свои:

```env
NEXT_PUBLIC_SUPABASE_URL=https://xyzabc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### Шаг 5: Запусти приложение

```bash
cd "c:\Users\mm597\Desktop\8 марта\postcards-quest"
npm run dev
```

Приложение откроется на **http://localhost:3000** 🎉

---

## 🧪 Тестирование локально

### Тест 1: Создание открытки
1. Нажми кнопку "Создать открытку" на главной странице
2. Заполни:
   - Имя отправителя (опционально)
   - Имя получателя (опционально)
   - Поздравление (обязательно)
   - Комплименты (по умолчанию 8, можно менять)
3. Выбери тему и режим
4. Нажми "Создать" → ты будешь перенаправлен на открытку

### Тест 2: Прохождение квеста
1. На странице открытки:
   - Увидишь красивый hero с кнопкой "Начать!"
   - Нажми "Начать!" → начнётся квест
   - Задание 1: выбери "Подснежник"
   - Задание 2: нажми на правильный ответ (меняется рандомно)
   - Задание 3: 8 раз кликни на сердечко ❤️
2. После квеста:
   - Раскроется поздравление
   - Появится букет (8 лепестков)
   - Кликни на лепесток → раскроется комплимент

### Тест 3: Секретное слово
1. На странице revealed (после квеста)
2. Внизу есть поле "Знаешь секретное слово?"
3. Введи слово по умолчанию: **весна**
4. Нажми ✓ → раскроется мемный экран

### Тест 4: Печать
1. На странице revealed нажми кнопку "Печать"
2. Откроется диалог печати (Ctrl+P)
3. Нажми "Печать" или "Сохранить в PDF"

---

## 🐛 Если что-то не работает

### `npm install` ошибка
```bash
# Попробуй:
npm ci --force
# или очисти кэш:
npm cache clean --force && npm install
```

### Supabase говорит "credentials not configured"
- Проверь `.env.local` — там должны быть правильные значения
- Перезагрузи dev-сервер: `npm run dev` (Ctrl+C → Enter)
- Убедись что эти значения начинаются с `NEXT_PUBLIC_` в браузере видны

### "Открытка не найдена" на `/card/[id]`
- Проверь ссылку в адресной строке
- Убедись что RLS-политики включены в Supabase (SQL Editor → проверь `alter table public.postcards enable row level security;`)
- Проверь что таблица была создана (`SELECT * FROM postcards LIMIT 1;`)

### Ошибка TAR при npm install
Это OK, это просто warning про Framer Motion. Приложение всё равно работает.

---

## 🌍 Деплой на Vercel

### Способ 1: GitHub + Vercel
```bash
# 1. Залей на GitHub
git init
git add .
git commit -m "first commit"
git remote add origin https://github.com/твойнейм/postcards-quest
git push -u origin main

# 2. На vercel.com → Import → выбери repo → Deploy
# 3. Добавь переменные окружения в Vercel:
#    NEXT_PUBLIC_SUPABASE_URL
#    NEXT_PUBLIC_SUPABASE_ANON_KEY
```

### Способ 2: Vercel CLI
```bash
npm i -g vercel
vercel login
vercel --prod
```

---

## 📁 Главные файлы для понимания

| Файл | Что там |
|------|---------|
| `app/page.tsx` | Главная страница лендинга |
| `app/create/page.tsx` | Форма создания открытки |
| `app/card/[id]/page.tsx` | **Главная логика**: квест + букет + пасхалка |
| `lib/memes.ts` | Все варианты квеста и секретное слово |
| `components/QuestFlow.tsx` | Логика 3-х заданий |
| `components/Bouquet.tsx` | Букет из 8 комплиментов |
| `.env.local` | Ключи Supabase (не коммитить!) |

---

## 📊 Что происходит за кулисами

### Создание открытки
1. Пользователь заполняет форму `/create`
2. Генерируется `quiz_seed` (число для детерминации ответов)
3. Данные сохраняются в `PUBLIC.POSTCARDS` таблице
4. Пользователь перенаправляется на `/card/[id]` с UUID

### При открытии открытки
1. Загружается открытка из БД (SELECT по ID)
2. Показывается hero с кнопкой "Начать!"
3. При нажатии — 3 задания квеста
   - Задание 1: static (выбери подснежник)
   - Задание 2: deterministic (по seed выбирается правильный ответ)
   - Задание 3: counting (8 кликов)
4. После завершения — раскрывается:
   - Основное поздравление
   - Букет (8 лепестков)
   - Мемные реакции (если mode = cute_meme)
   - Ввод секретного слова

---

## ✨ Что тебе нравится в проекте

- ✅ **Полная приватность**: каждая открытка видна только тому, кто знает ссылку
- ✅ **Нет регистрации**: анонимное творчество
- ✅ **Мобильная оптимизация**: работает на телефоне
- ✅ **Красивые анимации**: Framer Motion делает UX приятным
- ✅ **Легко кастомизировать**: просто меняй текст в компонентах
- ✅ **Готово к продакшену**: Vercel, Supabase, все лучшие практики

---

## 💬 Чек-лист перед тем как показать другим

- [ ] Проверил что .env.local есть и заполнен правильно
- [ ] Прошел весь квест сам и убедился что всё работает
- [ ] Проверил что открытка сохраняется в БД (Supabase → Data)
- [ ] Открыл открытку в другом браузере по ссылке
- [ ] Проверил печать (Ctrl+P → PDF)
- [ ] Попробовал секретное слово 'весна'
- [ ] Проверил адаптив на мобильнике

---

**Готово! Теперь веб-приложение полностью работает локально. 🎉**

Если что-то непонятно — смотри [SETUP.md](./SETUP.md) для подробного разбора.

**Happy 8th of March! 🌸💖✨**
